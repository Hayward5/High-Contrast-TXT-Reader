# HC-TXT-Reader 跨平台閱讀體驗改善計畫

## 文件目的

本文件只定義後續修改的範圍、優先順序、驗收條件與交付方式，供維護者審核。此階段不修改 `reader.html` 的產品行為；待計畫獲得核准後，再以獨立 Pull Request 分階段實作。

## 現況摘要

HC-TXT-Reader 目前以單一 HTML 檔提供離線 TXT 載入、自適應分頁、閱讀進度、單一手動書籤、全文搜尋、鍵盤翻頁、TTS，以及字級和亮度調整。後續改善必須保留「無後端、無外部依賴、可下載後離線使用」的核心定位。

經初步檢查，優先需要處理的風險如下：

1. 開啟另一本書時未先重設閱讀位置，沒有已存進度的新書可能沿用前一本書的索引。
2. 進度以檔名作為唯一識別，同名但內容不同的檔案會互相覆蓋。
3. TXT 直接以瀏覽器預設方式解碼，Big5、GB18030 或 UTF-16 等常見小說編碼可能出現亂碼。
4. 排版變更後仍保留舊的分頁歷史，上一頁位置可能不符合新排版。
5. 手機缺少閱讀區點擊、滑動翻頁、動態 viewport 與 safe-area 處理。
6. 現有「亮度」以內容透明度實作，降低亮度時也會降低文字對比。
7. 控制項的可存取名稱、焦點管理、動態狀態通知與 reduced-motion 支援不完整。

## 設計原則

- **離線優先**：閱讀與基本設定不得依賴網路或帳號。
- **隱私優先**：小說內容預設只在使用者裝置內處理。
- **漸進增強**：新 API 不可用時，核心開檔、閱讀與進度功能仍需運作。
- **位置穩定**：以原始文字 offset 作為閱讀位置的唯一基準，不以頁碼作永久識別。
- **可存取性**：滑鼠、鍵盤、觸控與輔助技術都應能完成核心閱讀流程。
- **保留單檔交付**：即使未來將開發原始碼模組化，發佈產物仍需提供單一 HTML 版本。
- **小步交付**：每一階段使用獨立 PR，避免穩定性修正與大型介面改版混在一起。

## 範圍與優先順序

### 第一階段：閱讀正確性與資料安全（P0）

#### 1.1 換書狀態重設與邊界保護

實作內容：

- 選擇新檔案時，先停止 TTS，並將 `currentIndex`、`pageHistory` 與該頁衍生狀態重設。
- 載入已存進度後，將位置限制在合法文字範圍內。
- 對空檔案、只有空白的檔案與比舊閱讀位置短的檔案提供明確狀態。
- 避免負數長度、`NaN` 或超出全文範圍的索引進入分頁函式。

驗收條件：

- 先開啟長書再開啟沒有進度的短書，短書從開頭正常顯示。
- 開啟空檔案不會出現錯誤、無限迴圈或錯誤進度。
- 0%、50% 與 100% 跳轉都不會使索引越界。

#### 1.2 書籍識別與儲存格式版本化

實作內容：

- 以內容 fingerprint 取代單純檔名 key；候選資料包含檔案大小、最後修改時間及內容雜湊。
- 儲存資料改成具 `version`、`bookId`、`fileName`、`position` 與 `updatedAt` 的物件。
- 自動遷移現有 `txt_auto_<檔名>` 與 `txt_manual_<檔名>` 資料；遷移失敗不得阻止閱讀。
- fingerprint 尚未完成時顯示合理的載入狀態，避免使用錯誤進度。

驗收條件：

- 同名、不同內容的兩個 TXT 各自保有進度。
- 相同內容重新開啟後可恢復位置。
- 舊版已儲存的進度可被讀取或安全忽略，不造成頁面失效。

#### 1.3 常見 TXT 編碼

實作內容：

- 改以 `ArrayBuffer` 讀取檔案並偵測 BOM。
- 支援 UTF-8、UTF-16LE、UTF-16BE、Big5 與 GB18030 的手動選擇。
- 預設解碼結果出現大量替代字元時顯示非阻塞式亂碼提示與編碼選單。
- 切換編碼時重新解碼同一檔案，不要求再次選檔。

驗收條件：

- 測試樣本中的 UTF-8、UTF-8 BOM、UTF-16LE、Big5 與 GB18030 中文均可正確顯示。
- 無法判定編碼時由使用者選擇，不靜默破壞文字。
- 編碼切換後進度仍被限制在有效範圍。

#### 1.4 分頁位置一致性

實作內容：

- 把字元 offset 定義為 canonical reading position。
- 字級、行距、視窗大小或裝置方向改變時，使舊分頁歷史失效並依目前 offset 重新排版。
- 以實際 DOM 測量實作上一頁起點，取代只依容器面積與字級估算的 fallback。
- 明確定義進度百分比代表「目前頁首」或「目前可見位置」，介面及儲存採相同語意。

驗收條件：

- 改字級或旋轉裝置後，上一頁與下一頁沒有明顯跳段或重複大段內容。
- 連續前後翻頁可回到相同文字位置。
- 重新載入後恢復位置不依賴先前的螢幕尺寸。

### 第二階段：行動裝置閱讀體驗（P1）

#### 2.1 Viewport 與安全區域

- 使用動態 viewport 單位，並保留舊瀏覽器 fallback。
- 套用 `safe-area-inset-*`，避免控制項與文字被瀏海或 Home Indicator 遮住。
- 測試網址列展開／收合、虛擬鍵盤開啟與橫直向切換。

#### 2.2 觸控與 Pointer Events

- 閱讀區左側點擊上一頁、右側點擊下一頁、中央切換控制列。
- 提供左右滑動翻頁，設定移動距離與方向判定門檻。
- 文字選取、長按、搜尋面板及表單控制項不得誤觸翻頁。
- 提供停用手勢及交換左右點擊區的設定。

#### 2.3 可收合控制介面

- 手機常駐列只顯示核心操作，其餘設定移至底部抽屜或設定面板。
- 閱讀時控制列可自動隱藏，並可由中央點擊、Escape 或明確按鈕恢復。
- 互動控制項維持適當的觸控目標，不以縮小字體強行塞入單列。

#### 2.4 分頁與捲動模式

- 保留目前分頁模式。
- 增加連續垂直捲動模式並保存使用者偏好。
- 兩種模式使用同一個 canonical offset，切換後盡量維持相同閱讀位置。

驗收平台：

- iPhone Safari 最新穩定版。
- iPad Safari 最新穩定版。
- Android Chrome 最新穩定版。
- Android Firefox 最新穩定版。
- Windows/macOS 的滑鼠與觸控板操作不得因手勢功能退化。

### 第三階段：排版、主題與無障礙（P1）

#### 3.1 閱讀主題與排版

- 以高對比黑黃、深色、紙張色與亮色主題取代內容透明度式「亮度」。
- 增加行距、版心寬度、文字對齊及字型選項。
- 儲存個人設定；設定失效或版本升級時安全回到預設值。
- 使用適合中文的換行規則，避免任意拆分標點或西文單字。

#### 3.2 無障礙

- 所有圖示按鈕提供可存取名稱與清楚的 `focus-visible` 樣式。
- 搜尋及設定面板實作 `aria-expanded`、焦點移入、焦點循環及關閉後焦點復原。
- 以 `aria-live` 的非阻塞訊息取代一般狀態用的 `alert()`。
- TTS 控制提供可理解的啟用／停止狀態。
- 動畫尊重 `prefers-reduced-motion`。
- 在 200% 與 400% 縮放下仍能操作核心功能。

驗收方式：

- 僅使用鍵盤可完成開檔、翻頁、搜尋、跳轉、設定與書籤操作。
- 使用 VoiceOver 或 NVDA 進行至少一輪核心流程測試。
- 主題文字與背景對比符合 WCAG AA；高對比主題維持更高標準。

### 第四階段：小說導覽功能（P2）

- 支援多書籤、附近文字摘要、章名、註記與刪除。
- 以可配置規則偵測「第 N 章」、「卷 N」、「Chapter N」等章節標題。
- 增加章節清單、上一章／下一章及章內進度。
- 搜尋增加上一筆／下一筆、結果總量與超過上限提示。
- TTS 增加語音、語速、音高、音量、暫停與繼續；清楚標示瀏覽器能力差異。
- 大型檔案的章節分析與搜尋視需要移至 Web Worker。

### 第五階段：可選的跨裝置能力（P2）

- 保留單檔離線版，同時評估提供託管 PWA。
- PWA 可加入 manifest、離線快取與安裝體驗，但核心閱讀不得要求連線。
- 先提供進度、書籤與偏好的 JSON 匯出／匯入。
- 雲端同步列為選用功能；預設不傳送小說正文，並在實作前另行審查隱私與資料格式。

## 非目標

下列項目不包含在前三階段的承諾範圍內：

- EPUB、PDF、MOBI 等非純文字格式。
- 帳號系統、付費功能或強制雲端服務。
- DRM 或受保護內容解密。
- 把使用者的小說正文預設上傳到伺服器。
- 在一般網頁中控制裝置的實體螢幕亮度。
- 為追求 PWA 而停止提供單檔 HTML。

## 測試策略

### 自動化測試

- 將索引限制、進度計算、儲存 migration、編碼選擇及章節規則拆成可單元測試的函式。
- 建立瀏覽器整合測試，涵蓋開檔、換書、翻頁、調整排版、搜尋、書籤與重新載入。
- 對空檔、極短檔、超長單行、中英日文混排、Emoji 與包含 HTML 標記的純文字建立 fixture。
- 加入同名不同內容，以及 UTF-8、UTF-16、Big5、GB18030 fixture。

### 手動測試矩陣

| 平台 | 瀏覽器 | 重點 |
| --- | --- | --- |
| Windows | Edge、Chrome、Firefox | 鍵盤、編碼、TTS、高對比模式 |
| macOS | Safari、Chrome、Firefox | 檔案選擇、字型、TTS |
| iOS/iPadOS | Safari | safe-area、網址列、旋轉、手勢、語音 |
| Android | Chrome、Firefox | 手勢、檔案選擇、返回操作、語音 |
| Linux | Chrome、Firefox | 字型 fallback、鍵盤、離線使用 |

### 效能基準

- 先以 1 MB、10 MB、50 MB TXT 量測載入、首次分頁與搜尋時間。
- 長時間翻頁不得持續累積無上限 DOM 節點或事件監聽器。
- 搜尋或 fingerprint 計算不得讓介面長時間無回應；超過可接受門檻時改用分批處理或 Worker。
- 實際門檻應在第一階段取得基準數據後，於對應實作 PR 中定案。

## PR 與交付方式

計畫核准後，預計拆分為以下 PR，且每個 PR 都附測試結果與相容性說明：

1. `fix: reset and validate reading position when loading books`
2. `feat: add versioned book identity and progress storage`
3. `feat: support common TXT encodings`
4. `fix: make pagination history stable across layout changes`
5. `feat: improve mobile viewport, controls, and page gestures`
6. `feat: add reading modes, themes, and typography settings`
7. `a11y: improve names, focus management, and announcements`
8. 後續章節、書籤、搜尋、TTS 與 PWA PR 依再次確認的優先順序安排。

每個實作 PR 應遵守：

- 不順帶加入未經核准的大型功能。
- 說明儲存格式或使用行為是否改變。
- 新功能提供 fallback，或明確列出不支援的平台。
- 更新 README 的使用說明、限制與相容性資訊。
- 若有可感知的 UI 修改，附桌面與手機畫面截圖。

## 審核決策點

請維護者在開始實作前確認：

1. 是否同意依 P0 → P1 → P2 的順序交付？
2. 書籍 fingerprint 是否可接受使用 Web Crypto API 計算內容雜湊？
3. 第一批編碼是否確定為 UTF-8、UTF-16LE/BE、Big5 與 GB18030？
4. 手機點擊區是否採「左上一頁、右下一頁、中央控制列」作為預設？
5. 是否同意保留高對比黑黃主題，但改以較柔和的深色主題作一般預設？
6. 是否同意先提供 JSON 匯出／匯入，再另案評估雲端同步？

在以上決策獲得確認前，本計畫不授權開始產品行為的實作。
